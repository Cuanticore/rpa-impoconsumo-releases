name: ping
# Diagnostico temporal: comprobar si Actions ejecuta en este repo PUBLICO.
# Los repos publicos no consumen minutos, asi que si esto corre y los privados
# no, la causa son los minutos y no una politica de la organizacion.
on:
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions ejecuta en este repositorio."
